## Learning Notes
 
This project was built to practice:
- `useState` and state updates
- Why direct variable mutation (`counter = counter + 1`) doesn't trigger a re-render — you must use the setter function
- Function return values (a function with no `return` gives `undefined`)
- How React's virtual DOM and diffing avoid unnecessary re-renders